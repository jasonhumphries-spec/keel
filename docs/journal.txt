=== 2026-04-30 SESSION 2 (keel-personal-life-os-build continued) ===
Transcript: /mnt/transcripts/2026-04-30-14-34-38-keel-personal-life-os-build.txt

ONBOARDING: Chevron SVG logo; KEEL brass sign-in; email-type step; 14-day→7-day default;
SVG icons; live feed terminal; rotating tips; onSnapshot/duplicate fixes.
SCAN: S1→Haiku ($0.80/$4.00/M); same-day transients→quietly_logged; lastScanCompletedAt;
incremental fetch; live feed every 5 threads.
AUTH: Auto-scan skip <30min; auth/cancelled-popup-request suppressed.
CATEGORIES PAGE: "Keel already knows:" panel; smart placeholder hints; categoryDefaults.ts.
ITEM EXPANDED PANEL: Priority→signal bars (4 levels); optimistic local state.
ADMIN: Light theme; S1/S2 cost columns; 60s cache; parallelised reads; deleted/reset users.
AI ABSTRACTION: aiComplete.ts provider wrapper; AdminSettingsPanel switcher; /api/ai-config.
STATE: provider=haiku, ~$0.028/scan, ~22s, ~582 FB reads/scan.


=== 2026-05-01 SESSION 3 (gemini + admin drill-down + scan-fix + UX) ===

GEMINI: gemini-2.0→2.5-flash; thinking disabled (thinkingBudget:0); ~$0.0008–0.002/scan.
ADMIN: Full light theme; per-user scan drill-down (50 runs); reclassify column; /api/scan-runs.
SCAN ARCH REFACTOR: Removed client-side token passing; server reads OAuth token from Firestore.
ROOT CAUSE FIX: onClick={triggerScan} was passing React event as job param → circular JSON.
RECLASSIFY: "↺ Reclassify all" on categories page; uses stored aiTitle/aiSummary.
CATEGORISE MODAL: Back/forward nav; "Do the rest later"; "Ignore"→quietly_logged+manuallyIgnored.
IGNORED: "Quietly Logged"→"Ignored" in UI (Firestore status unchanged).
STATE: provider=gemini-2.5-flash, ~$0.0008–0.002/scan, ~20s.


=== 2026-05-01 SESSION 4 (vercel deployment + typescript fixes) ===
Transcript: /mnt/transcripts/2026-05-01-20-39-49-keel-deployment-typescript-fixes.txt

VERCEL: keel→www.jaison.app; keel-admin→keel-admin.vercel.app. Framework=Next.js.
TYPESCRIPT: manuallyIgnored:boolean; 'pending' in SignalCalendarStatus; thinking?:number optional.
CLOUD FUNCTION: https://gmailscan-wel4wongwa-ew.a.run.app (europe-west1, 60min, 512MiB).
IAM: allUsers → roles/run.invoker. Custom domain live. Google OAuth configured.
STATE: www.jaison.app LIVE, provider=gemini-2.5-flash.


=== 2026-05-10 SESSION 6 (background scanning + calendar + UI improvements) ===

BACKGROUND SCANNING (Phase 2):
- handleGmailNotification CF — Pub/Sub triggered; 90s debounce; calls /api/gmail/background-scan
- renewGmailWatches CF — scheduled every 6 days
- nightlyItemExpiry CF — 1am daily; past events → quietly_logged/overdue; 1-day grace
- POST /api/inbox-watch — enable/disable watch, set+merge on root doc
- POST /api/gmail/background-scan — History API thread discovery, S1 via scanUtils
- BackgroundScanToggle in Settings (PRO badge); watch fields on root users/{uid} doc
- job='background' in scanRuns; backgroundScan* counters in meta/usage
- Pub/Sub topic gmail-inbox-notifications configured; ADMIN_SECRET as Firebase secret
- Auto-scan on page load removed — background scanning handles new emails now

SCAN UTILS — PARITY:
- src/lib/scanUtils.ts — classifyThread(), decodeBody(), buildThreadContext()
- decodeBody() always extracts HTML structure (dates/times) even with plain text fallback
- All scan routes import from scanUtils: scan, background-scan, reanalyse
- Prompt improvements: conditional payments, primary amounts, event signals for
  informational notices, proximity scoring for upcoming events

CALENDAR MATCHING — 6 BUGS FIXED:
1. Token expiry (no refresh in calendar/check route)
2. Server lookup window 35→365 days
3. Client useCalendarSignals hook 30→365 days
4. Reader-access calendars excluded (shared/family cals like TJP Family)
5. Sender domain fallback matching (senderMatchesCalTitle)
6. checkAllCalendars toggle fires immediate re-check

CALENDAR ADD:
- calendarUtils.ts — buildCalendarUrl() shared across CalendarColumn, ItemExpandedPanel, CategoryGrid
- Time extraction: range/single/bare patterns; fallback = all-day
- Description = aiDetailedSummary → aiSummary → signal description
- Opens /calendar/r/eventedit (native Google Calendar editor)

UI:
- CategoryFilterContext provider at dashboard2/page.tsx level
- Category checkboxes in sidebar (select all/none/only); filters DashboardShell2 data
- BackgroundScanToast — seenIds approach, bottom-right, 5s auto-dismiss
- SessionBanner — expired token warning with re-auth button
- needsReauth in AuthContextType
- Sidebar: removed Dashboard 1.0, Awaiting Reply sub-item, priority filter sub-items
- Privacy policy: corrected AI provider (Gemini), background scanning disclosed,
  encryption section accurate, contact = privacy@jaison.app
- Onboarding: background scanning mentioned, privacy reassurance added

KEY BUG: active dashboard is DashboardShell2 at /dashboard2 (not DashboardShell/dashboard1)
KEY BUG: CategoryFilterProvider must be parent of component that consumes it

CURRENT STATE (end of session 6):
- Background scanning: LIVE, watch active (expiry May 17, renews automatically)
- Active provider: gemini-2.5-flash
- Cost: ~$0.0002/background scan, ~$0.0008–0.002/manual scan
- Firestore security rules: confirmed tight (uid==userId enforced at DB level)
