=== 2026-05-06 SESSION 5 (dashboard-2.0 + UX overhaul + scan/cal fixes) ===
Transcript: /mnt/transcripts/2026-05-06-19-13-08-keel-app-development.txt

DASHBOARD 2.0 — new default at /dashboard (redirects to /dashboard2):
- Step-based scrolling layout replaces category grid as default
- Steps: 1=Urgent, 2=Awaiting Reply, 3=High Priority, 4=Everything Else
- Sort-inbox panel appears above step 1 when uncategorised items exist
- Each section is a floating white card (borderRadius:16, box-shadow) on #eeeef0 bg
- Coloured 3px top border per section (rust/brass/blue/slate) — no wash tints
- Old Dashboard 1.0 preserved at /dashboard1
- Scan overlay: fixed full-screen blur (z-index:500) on mount, minimum 1.5s display
  - Covers entire UI including sidebar — no interaction while scanning
  - Outside mobile/desktop branch so renders on all breakpoints
- Dynamic padding: 25vh top+bottom while section 1 is centred
- "Awaiting Responses" added as step 2 (steel blue #4A7FA5) between urgent and replies
- Step numbering: urgent always = 1 regardless of triage panel presence

CATEGORISE MODAL:
- 3-column grid (was 2), compact 11.5px buttons, minHeight:44
- "Other" category hidden from picker — choosing Other defeated classification
- Dark bg bottom bar (surface-recessed) with border-top separating Leave/Ignore
- markItemClassified() — module-level Set for instant topbar count update
- manualCategory:true written on assign — scan never overwrites manual assignments
- Post-classification scan removed — was finding new items and re-inflating count
- Writes succeed instantly confirmed via console logging (was cat_other confusion)

SCAN FIXES:
- processedThreadIds: filter(Boolean) so undefined entries don't pollute Set
- existingItemIds fallback: match by computed itemId if threadId missing
- Self-healing: writes threadId back when matched via fallback
- lastMessageInternalDate stored on items — skip logic compares vs Gmail timestamp
  not Keel write time (fixes threads with new replies being skipped)
- Terminal status guard: done/archived/paid never overwritten by scan
- category never overwritten on isExisting update (was regression from earlier fix)

AUTH / SESSION:
- firebase.ts: prompt:'consent' (not 'select_account') — ensures refresh token returned
- explicit browserLocalPersistence — prevents Safari/Firefox session clearing
- Frequent logout root cause identified and fixed

CALENDAR:
- checkAllCalendars setting added to Settings → Calendar section
- Stored on account doc, read by calendarCheck.ts
- Fetches all writable calendars via calendarList API when enabled
- matchedCalendarName stored on signal — shown in expanded panel as "In [Name]"
- Signal calStatus now syncs from parent onSnapshot — no refresh needed after cal check
- visibilitychange listener on SignalPill + CalendarBadge: when user returns from
  Google Calendar tab, auto-triggers cal check and updates status to on_cal
- Reanalyse route now triggers cal check after writing signals (was requiring 2 passes)
- Onboarding: new Calendar step between Categories and Scan — "Primary only" vs
  "All connected calendars" — writes choice to account doc before first scan

AI PROMPT QUALITY:
- Never use "the user" — use real first names from thread
- NEXT STEP identifies who acts next by direction of most recent message
- Event signals: ONLY for confirmed upcoming dates — not declined/obstacle dates
- Awaiting signals: ONLY for genuinely open questions, not confirmed matters
- Applied to both Vercel scan route and Cloud Function

RE-ANALYSE:
- /api/gmail/reanalyse route: full S1 reclassification of single item
- Auto token refresh (getValidAccessToken) — checks tokenExpiresAt, refreshes if needed
- Returns {skipped:true} on 404 instead of error — deleted threads handled gracefully
- Triggers calendar check after signal write (no second pass needed)
- "Re-analyse" in ItemExpandedPanel ... menu — status feedback inline
- Re-analyse all active items available in admin Dev Operations card

NOTE TO SELF:
- userNote: string | null on KeelItem
- Textarea in expanded panel, saves on blur, "Saved ✓" confirmation
- Scan never touches userNote field

ITEM EXPANDED PANEL:
- Header subtitle shows all participants (not just senderName)
- "From" metadata shows all participants for multi-party threads
- Signal calStatus syncs live from parent onSnapshot

ADMIN CONSOLE (keel-admin):
- Dev Operations card: Trigger scan, Re-analyse all, Calendar check,
  Flush items+signals, Full account reset, Ping keel app
- Full account reset wipes all subcollections including accounts/account_primary
  so user goes through onboarding on next sign-in
- Error detail capture: 401 bails early with clear auth message, 404 counted as skipped
- KEEL_APP_URL env var routes operations to production keel app

ONBOARDING:
- Calendar step added (step 5 of 6): Primary only vs All connected calendars
- Saves checkAllCalendars to account doc before scan starts

PAYMENT HISTORY:
- detectedAmountPence field mismatch fixed: reanalyse route was writing detectedAmount
- docToSignal now reads both field names as fallback for existing data

CURRENT STATE (end of session 5):
- Dashboard 2.0 is default at jaison.app
- Active provider: gemini-2.5-flash, ~$0.0008–0.002/scan
- All major classification persistence bugs fixed
- Calendar auto-recheck working
- Admin dev ops fully functional (pending KEEL_APP_URL env var in keel-admin Vercel)

=== 2026-05-10 SESSION 6 (background scanning — Phase 2) ===

BACKGROUND SCANNING (GMAIL PUB/SUB):
- handleGmailNotification Cloud Function — receives Gmail Pub/Sub push notifications
- renewGmailWatches Cloud Function — scheduled every 6 days (Gmail watches expire at 7)
- POST /api/inbox-watch — enables/disables watch per user, calls Gmail watch()/stop() APIs
- POST /api/gmail/background-scan — processes changed threads, called by CF
- BackgroundScanToggle component — in Settings > Scanning section, PRO badge
- 90s debounce in CF prevents AI call storms on inbox bursts
- Max 10 threads per notification — cost comparable to single manual scan press
- Token auto-refresh in background-scan route (access token may expire between manual sessions)
- Root users/{uid} doc created on enable (set+merge) — required for CF email lookup
- Watch fields on root doc: autoScanEnabled, watchStatus, watchProvider, watchExpiry,
  watchHistoryId, lastBackgroundScanAt
- job: 'background' in scanRuns + backgroundScan* fields in meta/usage

INFRASTRUCTURE (already in place from Session 5):
- Pub/Sub topic: gmail-inbox-notifications in keel-6921a
- gmail-api-push@system.gserviceaccount.com granted Publisher role
- ADMIN_SECRET set as Firebase secret

BUGS FIXED DURING DEPLOYMENT:
- functions/src/index.ts: export gmailScan → handleGmailScan (correct export name)
- background-scan/route.ts: removed googleapis import (not in keel deps), used fetch
- background-scan/route.ts: fixed aiComplete signature (db first arg, not object)
- background-scan/route.ts: tokens in accounts/account_primary subcollection not root doc
- inbox-watch/route.ts: set+merge instead of update (root doc may not exist)
- emailProviders/gmail.ts: removed googleapis, rewrote with fetch

CLASSIFICATION PARITY:
- Extracted classifyThread() + runInBatches() + BUILTIN_DESCRIPTIONS + ClassificationResult
  into src/lib/scanUtils.ts
- scan/route.ts now imports from scanUtils.ts (duplicate code removed)
- background-scan/route.ts now uses classifyThread() from scanUtils.ts
- All scan types now use identical prompt, importance scoring, signal rules
- Improving scanUtils.ts improves manual, auto, and background scans simultaneously

CONFIRMED WORKING (tested):
- Toggle in settings enables watch, creates root user doc, writes watchHistoryId
- Gmail push notification received within ~30s of email arriving
- CF debounce working (90s)
- Background scan processed 1 thread, created new item in Firestore
- scanRun doc: job=background, aiCostUsd=$0.00018, inputTokens=479, outputTokens=188,
  durationMs=3324, model=gemini-2.5-flash

BACKLOG NOTES:
- googleapis vs fetch: emailProviders/gmail.ts uses fetch not googleapis for consistency.
  If Keel expands to use many Google APIs in one place, revisit. Backlog item added.
- Cost tracking: provider hardcoded as 'gemini-flash' in background-scan route.
  Should read active provider from aiComplete for accuracy when provider switches. TODO.

CURRENT STATE:
- Background scanning live and working at www.jaison.app
- Watch expiry: May 17, 2026 (auto-renews via renewGmailWatches scheduler)
- Active provider: gemini-2.5-flash, ~$0.0002/background scan (single email)
