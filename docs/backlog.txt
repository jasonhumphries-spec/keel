KEEL — BACKLOG
Last updated: 2026-05-10 Session 6
Status key: [ ] todo  [~] in progress  [x] done

═══════════════════════════════════════════════
CRITICAL / PRE-BETA
═══════════════════════════════════════════════

[ ] Google OAuth app verification
    Required before public beta. Currently unverified — works for ≤100 test users.
    Submit via Google Cloud Console → OAuth consent screen.

[ ] Security/GDPR doc (internal)
    UK GDPR compliance, data handling procedures, breach response. Required before
    real user testing beyond developer accounts.

[ ] Firestore indexes
    - scanRuns: scanAt desc (Firebase prompts with link on first query)
    - items: (status, receivedAt) — needed for reclassify quietly_logged query

[ ] Replace Anthropic API key usage note
    ANTHROPIC_API_KEY env var still present for fallback — ensure Gemini is confirmed
    as primary and document any remaining Anthropic usage.

═══════════════════════════════════════════════
INFRASTRUCTURE
═══════════════════════════════════════════════

[ ] Background scan: read active provider from /config/aiProvider
    background-scan/route.ts hardcodes model='gemini-2.5-flash' for cost tracking.
    Should call getActiveProvider(db) from aiComplete so cost log is accurate when
    provider is switched via admin panel.

[ ] Terminate active scan before dev reset
    AbortController on scan fetch in AuthContext; call from DevTools before data reset.

[ ] GCal API pagination for very busy calendars
    calendarCheck.ts uses maxResults=2500 (API hard limit). If a user has multiple
    busy calendars over 12 months this could truncate. Add pagination if needed.

[ ] Phase 3: Gmail Pub/Sub — background scan for Pro gating
    Currently available to all alpha users. Add Pro tier check in inbox-watch route
    before enabling for public users.

═══════════════════════════════════════════════
PERFORMANCE / COST
═══════════════════════════════════════════════

[ ] threadIds[] array on account doc
    Flat existence index to eliminate full items collection read per scan.
    Biggest remaining Firestore read saving (~582 reads/incremental scan currently).

[ ] Efficiency review
    Firestore reads per scan, batch sizes, onSnapshot listener counts.

[ ] AI cost review
    Prompt length in scanUtils.ts is substantial — audit token usage per classification.
    Current: ~$0.0008–0.002/manual scan, ~$0.0002/background (Gemini Flash).

[ ] DB cost review
    Read/write counts, index usage, data shape optimisation.

═══════════════════════════════════════════════
FEATURES
═══════════════════════════════════════════════

[ ] Beta landing page
    Partially built in a separate session. Needs completion + copy.

[ ] Email preview drawer
    Slide-in panel replacing "Open in Gmail". User preference toggle in settings.

[ ] Multi-account support
    Account switcher in sidebar (Slack-style). Multiple Gmail accounts per user.

[ ] Snooze + sidebar views (Overdue, Payment Due, Snoozed, Archived)
    Designed in session 5 design doc. snoozedUntil + preSnoozePriority on KeelItem.
    Sidebar filter views for Overdue, Payment Due, Snoozed, Archived.

[ ] Alpha code gate
    Post sign-in gate. Code: HELM3X (env var). Designed in session 5, not built.

[ ] Version display in sidebar footer
    "Keel · Alpha · v0.x.x" from NEXT_PUBLIC_APP_VERSION. Designed, not built.

[ ] Themes — reduce to 3
    Harbour (default), Slate (cool professional), Obsidian (dark premium).
    Designed in session 5 design doc. Currently 10 themes.

[ ] Calendar duplicate deduplication in calendar column
    Same item with EVENT + RSVP signals on same date shows twice. Deduplicate
    same-item same-date signals in calendar column display.

[ ] Card calendar badge × glyph
    After re-analyse + calendar match, card badge sometimes shows × (ignored) rather
    than teal ✓ due to stale local state. Investigate onSnapshot sync.

═══════════════════════════════════════════════
CODE QUALITY
═══════════════════════════════════════════════

[ ] Delete dashboard1/DashboardShell.tsx
    Active dashboard is DashboardShell2. dashboard1 page + DashboardShell.tsx are
    orphaned and can be removed.

[ ] Standardise docToItem pattern
    Multiple pages have local docToItem copies. Centralise.

[ ] Code review
    Structure, patterns, remaining dead code.

[ ] Field-level encryption (V2 — post public launch)
    Fields to encrypt: aiTitle, aiSummary, aiDetailedSummary, senderName,
    senderEmail, subject, signal descriptions, categoryHints.
    Leave unencrypted (needed for queries): status, categoryId, aiImportanceScore,
    receivedAt, signals.type/date/amount.
    Architecture: key pair at onboarding; public key in Firestore; server encrypts
    before writing; client decrypts on read. ~6 sessions effort.
    Decision: after public launch, when privacy positioning needs to be airtight.

[ ] Gemini billing confirmed for production scale
    Free tier quota = 0 for some endpoints. Ensure billing enabled.

[ ] googleapis vs fetch — review if Google API surface expands
    emailProviders/gmail.ts uses fetch (consistent with codebase).
    Revisit if Keel adds Drive, Sheets, or many Calendar endpoints.

═══════════════════════════════════════════════
DONE
═══════════════════════════════════════════════

[x] Background scanning via Gmail Pub/Sub ← Session 6
    handleGmailNotification + renewGmailWatches + nightlyItemExpiry CFs
    /api/inbox-watch + /api/gmail/background-scan
    BackgroundScanToggle in settings, toast notification, auto-scan on load removed
[x] scanUtils.ts — shared classifyThread(), all scan types at parity ← Session 6
[x] Calendar matching — 6 bugs fixed ← Session 6
    Token refresh, 365-day window (server+client), reader cals, sender domain,
    high-entropy word matching, toggle re-check
[x] calendarUtils.ts — shared buildCalendarUrl(), time extraction, correct description ← Session 6
[x] Nightly item expiry Cloud Function ← Session 6
[x] Category filter in sidebar — checkboxes, all/none/only, filters grid+calendar ← Session 6
[x] CategoryFilterContext at page level (dashboard2/page.tsx) ← Session 6
[x] SessionBanner for expired tokens ← Session 6
[x] BackgroundScanToast — seenIds approach ← Session 6
[x] Prompt improvements: conditional payments, primary amounts, event signals,
    proximity scoring, reanalyse guard against quietly_logged ← Session 6
[x] Privacy policy updated — correct AI provider, background scanning, accurate encryption ← Session 6
[x] Onboarding copy updated — background scanning, privacy reassurance ← Session 6
[x] Sidebar cleanup — removed Dashboard 1.0, Awaiting Reply, priority filter sub-items ← Session 6
[x] Dashboard 2.0 — step-based layout, DashboardShell2 ← Session 5
[x] manualCategory flag — scan never overwrites user category assignments ← Session 5
[x] Terminal status guard — done/archived/paid never resurrected by scan ← Session 5
[x] Re-analyse single item ← Session 5
[x] Note to self — userNote field on KeelItem ← Session 5
[x] All connected calendars setting ← Session 5
[x] Calendar auto-recheck on tab return ← Session 5
[x] Admin Dev Operations card ← Session 5
[x] Deploy keel to Vercel (www.jaison.app) ← Session 4
[x] Deploy keel-admin to Vercel (keel-admin.vercel.app) ← Session 4
[x] Cloud Function (gmailScan) deployed + IAM fixed ← Session 4
[x] Custom domain www.jaison.app live ← Session 4
[x] Gemini 2.5 Flash integration + cost confirmed ← Session 3
[x] Scan run logging (users/{uid}/scanRuns) ← Session 3
[x] Admin per-user drill-down ← Session 3
[x] Reclassify all ← Session 3
[x] Categorise modal (nav, ignore, bail-out) ← Session 3
[x] AI provider abstraction (aiComplete.ts) ← Session 2
[x] Onboarding overhaul (email type, SVG icons, live feed) ← Session 2
[x] Signal bars priority UI ← Session 2
