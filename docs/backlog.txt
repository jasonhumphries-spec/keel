KEEL — BACKLOG
Last updated: 2026-05-13 Session 6
Status key: [ ] todo  [~] in progress  [x] done

═══════════════════════════════════════════════
⚠ CHECK TOMORROW (2026-05-14)
═══════════════════════════════════════════════

[ ] Verify nightlyItemExpiry fired correctly overnight
    Greenhouse Bootcamp Pre-Task (Undaunted category) has EVENT + DEADLINE on
    7 May — 6 days past. Should have been expired to 'overdue'. Still showing
    as 'awaiting_action' on 13 May.
    Likely cause: backgroundScan.ts had many session-6 changes (snooze wake-up,
    proximity rescore, chunked deletes) that may not all be live in the CF.
    ACTION: firebase deploy --only functions --project keel-6921a
    Then check tomorrow that stale past-event items are correctly expired/overdue.

═══════════════════════════════════════════════
CRITICAL / PRE-BETA
═══════════════════════════════════════════════

[ ] Google OAuth app verification
    Required before public beta. Currently unverified — works for test users only.
    Submit via Google Cloud Console → OAuth consent screen.

[ ] Security/GDPR doc
    UK GDPR compliance, data handling, retention policy.

[ ] Replace privacy@keel.app placeholder
    Also update LAST_UPDATED date in privacy policy page.

[ ] Firestore indexes
    - scanRuns: scanAt desc
    - items: (status, receivedAt) — reclassify quietly_logged query
    - items: createdAt desc — BackgroundScanToast item fetch

[ ] Centralise docToItem
    Multiple local copies in awaiting-reply/page.tsx, quietly-logged/page.tsx,
    Topbar.tsx — each needs manual updates when KeelItem fields change (caused
    repeated build failures in session 6). Extract to src/lib/docToItem.ts.

═══════════════════════════════════════════════
INFRASTRUCTURE
═══════════════════════════════════════════════

[ ] Full Cloud Functions redeploy (do today)
    firebase deploy --only functions --project keel-6921a
    Multiple backgroundScan.ts changes this session may not all be live.

[ ] Onboarding scan failure root cause
    New user CF call failed ~13 May 11:00 UTC. Check gmailScan CF logs.
    Possibly cold-start timeout. Fix: CF fallback to Vercel now in place.

[ ] Multi-account support
    Account switcher in sidebar (Slack-style).

═══════════════════════════════════════════════
PERFORMANCE / COST
═══════════════════════════════════════════════

[ ] threadIds[] array on account doc
    Eliminate full items collection read per scan (~582 reads currently).

[ ] Efficiency review — Firestore reads, batch sizes, onSnapshot listeners.
[ ] AI cost review — prompt lengths, token usage, compression.
[ ] DB cost review — read/write counts, index usage, data shape.

═══════════════════════════════════════════════
FEATURES
═══════════════════════════════════════════════

[ ] Email preview drawer
    Slide-in panel replacing "Open in Gmail".

[ ] Beta landing page
    Partially built. Needs completion + copy.

[ ] Bulk re-analyse awaiting_reply items
    The awaiting_reply vs awaiting_action prompt was fixed this session but
    existing items still have old classifications. Add a bulk re-analyse button
    in admin Dev Ops for all awaiting_reply items.

[ ] Promotions/Social/Spam — background scanning limitation
    Email scope settings added for manual scans only. Background scanning stays
    INBOX-only (Gmail History API limitation). Document clearly in settings UI.

═══════════════════════════════════════════════
CODE QUALITY
═══════════════════════════════════════════════

[ ] Code review — many files patched across multiple sessions.
[ ] Field-level encryption (V2) — before public launch.
[ ] Gemini Flash billing — ensure enabled for production scale.

═══════════════════════════════════════════════
DONE — Session 6 (2026-05-12/13)
═══════════════════════════════════════════════

[x] Background scanning via Gmail Pub/Sub — WORKING
[x] Fix: email written to root users/{uid} doc (CF can now find user by email)
[x] Fix: scan routing — CF for onboarding only, Vercel for manual/auto
[x] Fix: CF fallback to Vercel route if Cloud Function unreachable
[x] Fix: full account reset — chunked deletes (>500 docs), root doc cleared
[x] Fix: OAuth token shared utility (tokenUtils.ts) — auto-refresh across all routes
[x] Fix: background-scan route uses shared scanUtils + correct account path
[x] Fix: awaiting_reply vs awaiting_action prompt rule sharpened
[x] Fix: event proximity scoring — within 7 days always lands as High
[x] Nightly proximity re-scoring — items escalate as event date approaches
[x] Nightly snooze wake-up — clears snoozedUntil, restores priority
[x] Snooze on card — 1d/3d/7d picker, collapsed rendering, sorted last
[x] All Mail page — every scanned email, Sent/status filters, search
[x] All Mail: isOutbound field + Sent badge + Chase filter
[x] All Mail: click status badge → jump to dashboard item
[x] Dashboard highlight — ?highlight=itemId, scroll + brass ring
[x] Dashboard highlight — handles off-dashboard items (ignored etc)
[x] Search: View → dashboard, Gmail → new tab
[x] Quick Done button on card hover
[x] Just in! badge — teal border + bg tint for fresh background-scanned items
[x] Freshness indicator in topbar — Live · 2m ago / Updated 1h ago
[x] Background scan toast — sender, title, category, priority, status
[x] CategoriseModal: stale closure fix (no more infinite loop)
[x] CategoriseModal: auto-classify similar items (same sender / title overlap)
[x] Onboarding: inline description when adding custom category
[x] Onboarding: error + timeout handling (no more infinite spinner)
[x] Admin: dropdown shows name · email
[x] Gmail watch renewal every 5 days (was 6)
[x] Email scope settings — opt-in Promotions/Social/Spam (Pro feature)
[x] Scan includes sent folder — {in:inbox in:sent}
[x] DIRECTION note in AI prompt for outbound/self-email threads

═══════════════════════════════════════════════
DONE — Sessions 1–5
═══════════════════════════════════════════════

[x] Deploy keel + keel-admin to Vercel
[x] Custom domain www.jaison.app
[x] Cloud Function deployed + IAM fixed
[x] Gemini 2.5 Flash as active provider
[x] Scan run logging + admin drill-down
[x] Reclassify all + categorise modal
[x] Priority dot + coloured left border
[x] Calendar column redesign + card badges
[x] Sidebar filter views (Overdue, Payment Due, Snoozed, Archived)
[x] Version display + alpha code gate
[x] Background scan toggle in Settings
[x] 3 themes (Harbour/Slate/Obsidian)
[x] Onboarding overhaul (email type, live feed, thread volume reaction)
[x] AI provider abstraction + admin provider switcher
